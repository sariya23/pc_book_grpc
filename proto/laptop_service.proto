syntax = "proto3";

package pc;
option go_package = "./pb";

import "laptop.proto";

message CreateLaptopRequst {
    Laptop laptop = 1;
}

message CreateLaptopResponse {
    string id = 1;
}

service LaptopService {
    rpc CreateLaptop(CreateLaptopRequst) returns (CreateLaptopResponse) {};
}